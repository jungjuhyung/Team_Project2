<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="placerwish_t">
	
	
	<select id="placeRDetail" resultType="placerwishvo">
		select * from placewish_t where place_idx = #{place_idx}
	</select>
	
</mapper>
















